export class Ba {

  id: string;
  number: string;
  cvv: number;
  validity: string;
  balance: number;

  // static cloneBase(roles: Role): Role {
  //   let clonedRoles: Role = new Role();
  //   clonedRoles.id = roles.id;
  //   clonedRoles.name = roles.name;
  //   return clonedRoles;
  // }
}
